export default function userInfos(state = {}, action) {
  // switch (action.type) {
  //   case 'INFOS_USER':
  //     return { state: {} };

  //   default:
  //     return state;
  // }
  // console.log(action);
  return state;
}
